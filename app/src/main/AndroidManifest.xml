<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.darwishashraf.flashchatnewfirebase">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_rokok"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PeringatanActivity"></activity>
        <activity android:name=".MotivasiActivity" />
        <activity android:name=".KenaliTandaKemurunganActivity" />
        <activity android:name=".TidakKembaliMerokokSemulaActivity" />
        <activity android:name=".PersediaanUntukBerhentiActivity" />
        <activity android:name=".MengapaIaSangatSukarActivity" />
        <activity android:name=".MengapaPerluBerhentiActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".KKSriGadingMaps"
            android:label="@string/title_activity_kksri_gading_maps" />
        <activity android:name=".KKSriGadingActivity" />
        <activity
            android:name=".HospitalKualaLumpurMaps"
            android:label="@string/title_activity_hospital_kuala_lumpur_maps" />
        <activity android:name=".HospitalKualaLumpurActivity" />
        <activity
            android:name=".HospitalAlorSetarMaps"
            android:label="@string/title_activity_hospital_alor_setar_maps" />
        <activity android:name=".HospitalAlorSetarActivity" />
        <activity
            android:name=".HospitalBatuPahatMaps"
            android:label="@string/title_activity_hospital_batu_pahat_maps" />
        <activity android:name=".HospitalBatuPahatActivity" />
        <activity
            android:name=".KKParitRajaMaps"
            android:label="@string/title_activity_kkparit_raja_maps" />
        <activity android:name=".KKParitRajaActivity" />
        <activity android:name=".DapatkanBantuanActivity" />
        <activity android:name=".MainIntroActivity" />
        <activity android:name=".MusicActivity" />
        <activity android:name=".MainSidebarActivity" />
        <activity android:name=".SplashMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".RegisterActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainChatActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden|adjustResize" />
        <activity android:name=".IntroActivity" />
        <activity android:name=".NoteActivity" />
        <activity android:name=".NotesListActivity" />
    </application>

</manifest>